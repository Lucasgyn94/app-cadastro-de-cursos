// classe
export class Curso { 
    // O ? indica que a variável idCurso é opcional, pode ter um número ou ser undefined
    // atributos
    nomeCurso: string = "";
    valorCurso: number = 0;
    idCurso?: number;

}